name: My Workflow

on:
  push:
    branches: [ main ]

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: My Step
        run: echo "My username is ${{ secrets.USERNAME }} and my password is ${{ secrets.PASSWORD }}"
